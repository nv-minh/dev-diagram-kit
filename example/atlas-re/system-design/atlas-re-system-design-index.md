---
type: system-design-index
feature: atlas-re
status: example
updated: 2026-07-26
---

# Atlas Re — system design (C4)

Anonymized reinsurance underwriting platform. See [`../DOMAIN.md`](../DOMAIN.md).

| Level | File | Main blocks |
|---|---|---|
| L1 Context | [`atlas-re-context.d2`](./atlas-re-context.d2) → `.svg` | Underwriter, Broker, Atlas Re, Azure AD, Azure Blob, Currency API |
| L2 Container | [`atlas-re-container.d2`](./atlas-re-container.d2) → `.svg` | Web, API Gateway (hexagon), services, PostgreSQL (cylinder), Redis (stored_data), Kafka (queue) |

**Shapes exercised (per `rules/node-shapes.md`):** API Gateway = `hexagon`, PostgreSQL = `cylinder`,
Redis = `stored_data`, Kafka = `queue`, external systems = dashed. Redis + Kafka are fabricated (proposed).

Regenerate:
```bash
bash skills/d2-activity/render.sh example/atlas-re/system-design/atlas-re-context.d2
bash skills/d2-activity/render.sh example/atlas-re/system-design/atlas-re-container.d2
```

> A dark-theme HTML presentation (Copy/PNG/PDF) can be generated by running the `/system-design` skill
> on this folder (it inlines the two SVGs into `c4-export-template.html`) — omitted here to keep the
> example light; the `.svg` files are the core deliverable.
